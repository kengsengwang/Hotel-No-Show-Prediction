from __future__ import annotations
import pandas as pd
from pandas import DataFrame

def engineer_features(df: DataFrame) -> DataFrame:
    """Add features here. Currently pass-through."""
    return df.copy()
